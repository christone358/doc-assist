## 1. 协议建模

- [x] 1.1 定义最小委派 handoff 与最小结构化执行结果模型，明确主 Agent 只传递调度决定与连续性记忆，子 Agent 只回传状态、摘要和失败/恢复信号
- [x] 1.2 定义执行链路事件模型，覆盖规划、调度、资源读取、澄清、完成和失败等阶段
- [x] 1.3 明确主子上下文隔离边界，区分哪些信息属于主 Agent 结果、哪些属于领域状态 patch、哪些仅保留在子 Agent 内部

## 2. Orchestrator 提示词与工具声明重构

- [x] 2.1 重写 `document_agent` 的 system prompt，使其从“硬编码工具路径”转为“编排职责 + 工具分级 + 结构化结果消费”
- [x] 2.2 调整主 Agent 的工具声明，只保留其编排所需的公共工具、委派工具和必要澄清工具
- [x] 2.3 调整 Skill Sub-agent 的提示词和工具声明，使其聚焦执行、资源访问和结果回传
- [x] 2.4 为主 Agent 和子 Agent 分别梳理提示词模板与职责边界，避免重复或冲突
- [x] 2.5 将主 Agent 与子 Agent 的系统 prompt 从 Python 内联字符串迁移到独立文本模板，并保留运行时插槽注入能力
- [x] 2.6 明确第一阶段的宿主侧工具接入边界：先落地 `catalog` 与 `tool view`，统一 `tool gateway` 留作后续演进

## 3. Host 侧 MCP Catalog / Tool View 落地

- [x] 3.1 定义 `catalog` 数据模型，至少覆盖 tool id、来源 server、逻辑名称、可见性、适用 agent 角色和 schema 元数据
- [x] 3.2 基于当前已实现的 MCP server 工具，构建宿主级 catalog 初始化逻辑，先纳管 `facts.*`、`docs.*`、`skill.*`
- [x] 3.3 定义主 Agent `tool view` 生成规则，只暴露公共工具集、委派工具和必要澄清工具
- [x] 3.4 定义 Skill Sub-agent `tool view` 生成规则，暴露执行所需的内部工具与必要公共工具
- [x] 3.5 用 catalog / tool view 替换当前按 agent 手写 wrapper 列表的接入方式，保证行为与现有 MCP server 顺接
- [x] 3.6 为 catalog / tool view 增加自动化测试，覆盖工具收敛、角色筛选和可见性边界
- [x] 3.7 接入基于 `list_tools` 或等价发现接口的宿主侧工具发现流程，使 `catalog` 初始化不再依赖手写工具名清单
- [x] 3.8 将当前固定 `ordered_ids` / 静态 schema 映射收敛为“发现结果 + 宿主策略 + 稳定排序规则”，避免新增 public MCP 工具时必须同步修改主 Agent / 子 Agent 的固定枚举
- [x] 3.9 将按-tool 手写 wrapper 进一步收敛为可复用的通用 MCP 调用适配层，只保留角色策略、显示文案和必要的上下文写入差异
- [x] 3.10 验证新增一个 public MCP 工具后，仅需在 runtime / server 注册并满足宿主策略，即可自动进入对应 Agent 的 `tool view`

## 3A. Prompt 工具声明与动态注入

- [x] 3A.1 为主 Agent / 子 Agent 定义从 `tool view` 和 schema 元数据生成“可用工具”说明的运行时注入逻辑
- [x] 3A.2 调整提示词模板，移除长期手写维护的固定工具清单，改为使用动态注入的工具说明块
- [x] 3A.3 明确工具说明中的展示字段规范，至少覆盖工具名、适用职责、关键参数和必要使用边界，避免模板与真实 schema 脱节
## 4. 主子通信与执行链路适配

- [x] 4.1 调整 `execute_skill` 的返回协议，使其输出最小结构化执行结果；草稿和文档标识等领域状态继续通过 state 传递，而不只返回自由文本摘要
- [x] 4.2 在 `runner_adapter` 或等价层中接入结构化执行链路事件的采集与转发
- [ ] 4.3 扩展主 Agent 状态更新逻辑，使其基于最小结构化 Skill 结果决定继续、追问、重试或完成，而不是消费子 Agent 执行细节清单
- [x] 4.4 保持子 Agent 独立上下文执行，并验证主 Agent 不读取完整子 Agent 推理历史

## 5. 对话记录与展示适配

- [x] 5.1 扩展 conversation 记录结构，分别保存结构化执行结果、领域状态快照和链路事件
- [x] 5.2 调整前端或事件推送契约，使 Web UI 能展示“规划 -> 调度 -> 执行 -> 完成/失败”的链路
- [ ] 5.3 明确展示层过滤规则，确保展示的是执行链路摘要而不是原始内部 reasoning 文本

## 6. 回归与验证

- [ ] 6.1 为主子结构化通信协议增加自动化测试，覆盖成功、失败、澄清和中断场景
- [ ] 6.2 为 Orchestrator 提示词与工具声明分层增加回归测试，确认主 Agent 不再下沉执行细节
- [x] 6.3 为上下文隔离增加验证，确认子 Agent 内部执行历史不会直接泄漏到主对话记录
- [ ] 6.4 为执行链路展示增加端到端验证，确认前端能够稳定展示结构化阶段事件
- [x] 6.5 为 discovery-driven catalog 增加回归测试，覆盖“新增 public 工具自动进入 catalog / tool view”和“internal 工具不会误暴露给主 Agent”
- [x] 6.6 为 prompt 工具说明增加一致性测试，确认模板中注入的工具清单与实际 `tool view` 完全一致，不存在遗漏、越权或陈旧声明
