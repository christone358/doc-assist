## 1. 执行对象事件契约

- [x] 1.1 在 `backend/agent/models.py` 中新增或扩展执行对象节点结构，覆盖 `llm_thought`、`tool_call`、`skill_call` 的稳定标识、父子关系和详情字段
- [x] 1.2 将 `user_question` 和 `system_state` 纳入统一对象模型，覆盖等待用户回复、完成和失败等关键状态
- [x] 1.3 重构 `backend/agent/adk/runner_adapter.py` 的执行事件发射逻辑，为主 Agent 和子 Agent 统一生成对象节点标识与状态更新
- [x] 1.4 为问题节点、系统状态节点补充归属规则，明确何时挂在 Skill 下、何时保留顶层展示
- [x] 1.5 调整 `backend/main.py` 与流式转发链路，使前端能够接收到对象化过程事件，并保留旧事件的兼容过渡
- [x] 1.6 为失败、完成、等待用户回复等状态建立显式映射表，明确哪些只更新父节点状态，哪些生成独立 `system_state`

## 2. 工具与 Skill 观测数据补齐

- [x] 2.1 更新 `backend/agent/adk/fact_tools.py`、`backend/agent/adk/mcp_tools.py` 等资源工具，为每次工具调用补充输入、输出摘要和可折叠详情字段
- [x] 2.2 为工具观测数据增加可展示化处理，统一生成脱敏、摘要化、截断后的 `display_input`、`output_preview`、`output_detail`
- [x] 2.3 更新 `backend/agent/adk/execute_skill_tool.py`，将 Skill 选择原因、交接摘要和子执行过程绑定到同一个 Skill 对象节点
- [x] 2.4 为 `write_document` 等正式正文类工具增加特殊处理，只输出摘要和元信息，不把完整正文复制到右侧过程详情
- [x] 2.5 明确思考节点的数据来源和安全边界，确保只发送可展示思考记录，不直接暴露 raw reasoning
- [x] 2.6 为所有执行对象补充稳定的创建顺序字段，确保前端不依赖接收顺序猜测排序
- [x] 2.7 为过程明细字段建立统一预算控制和截断标记策略，覆盖 WebSocket 消息与会话历史持久化

## 3. 前端过程明细重构

- [x] 3.1 重构 `frontend/src/lib/stores.js` 和 `frontend/src/lib/components/Chat.svelte` 的过程事件归一化逻辑，优先消费对象化节点数据
- [x] 3.2 重写 `frontend/src/lib/components/ObservabilityPanel.svelte`，按 `LLM 思考 / 工具调用 / Skill 调用` 三类对象渲染右侧过程明细
- [x] 3.3 为工具卡片实现“工具名称 + 状态”标题，以及输入、输出摘要、详情折叠区
- [x] 3.4 为 Skill 卡片实现父子层级展示，确保 Skill 下的思考和工具调用按容器归属呈现
- [x] 3.5 为 `user_question` 和 `system_state` 增加对象化渲染，避免在新面板里丢失澄清、完成和失败状态
- [x] 3.6 明确顶层与 Skill 容器的单一归属规则，避免同一对象双重渲染
- [x] 3.7 按对象创建顺序渲染顶层对象和 Skill 子对象，禁止按类型重排
- [x] 3.8 保留旧事件回退归一化逻辑，确保历史轮次在新面板中只读可见
- [x] 3.9 确保流式内容补充或状态更新时对象位置不跳动

## 4. 验证与回归

- [ ] 4.1 为后端执行对象事件补充测试，覆盖重复工具调用、嵌套 Skill、失败状态和详情字段
- [ ] 4.2 为前端归一化与右侧面板补充测试，确认对象分组、折叠行为、单一归属和兼容回退逻辑正确
- [ ] 4.3 增加高体积输入输出、脱敏字段和正式正文类工具的验证，确认右侧只展示可展示版本
- [ ] 4.4 增加对象排序验证，确认顶层对象和 Skill 子对象均按创建顺序稳定展示
- [ ] 4.5 增加历史轮次回放验证，确认旧格式对话在新面板中仍可读
- [ ] 4.6 验证流式更新过程中对象位置稳定，不因状态补充而跳动
- [ ] 4.7 手动验证普通问答、事实查询、Skill 写作、澄清中断和文档生成场景，确认右侧过程明细符合新方案
